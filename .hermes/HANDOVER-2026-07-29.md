# Handover — 2026-07-29 → Next Session

## Status: Clean Build ✅ | Branch: `pipeline-rework`

### Last 13 Commits (newest first)
| Commit | What |
|--------|------|
| `be64164` | EditorTickScheduler: skip during MouseDrag + conditional RepaintAll → **fixes object movement** |
| `365a315` | Vector3.Abs → Mathf.Abs per component (CS0117) |
| `7b615b8` | Batch fix: EventType.Command, Slider args, EditAnchor struct init, RebuildDirty → Rebuild() |
| `f004d1c` | RebuildDirty callers + EventType.Command in EditorTickScheduler |
| `e1ccf03` | CS0246 BufferAsEditView + removed dead RebuildDirty method |
| `e6e4bca` | Remove duplicate VisualOutput creation in InitializePipeline |
| `4b46825` | Missing `using System.Collections.Generic` + formatting |
| `463fd9c` | 1mm epsilon threshold on CheckModelTransformChanged (float drift) |
| `244ab0c` | RemoveObjectCommand stores bounds → partial rebuild on undo |
| `c7cf6f4` | Move undo/redo into EditorTickScheduler with selection guard |
| `a9644c7` | ProcessUndoRedo delegates to CommandStack.Undo()/Redo() |
| `08b643f` | ClearAllCommand + wire into ClearObjects() |
| `4e93a56` | Remove double rebuild in RemoveLastObject |

### What was done this session
- **Undo/Redo architecture cleanup**: Stripped broken Undo hijacking from EditorTickScheduler that stole Ctrl+Z during transform changes. Replaced with proper `Undo.RegisterFullObjectHierarchyUndo` + `Undo.DestroyObjectImmediate`. CommandStack is now only triggered by explicit Push() calls (Add/Remove/Clear).
- **Editor movement regression fixed**: Two causes — `SceneView.RepaintAll()` every frame stole focus; EditorTickScheduler ran during mouse drag. Both patched: early-return on `MouseDrag`, repaint only when work was done.
- **ClearAllCommand**: New undo command for `ClearObjects()` — restores all objects in one Ctrl+Z step.
- **RemoveObjectCommand bounds fix**: Now stores affected bounds → partial rebuild on undo instead of full rebuild.
- **Compile clean**: Fixed CS0246, CS0117 (×3), CS1503 (×2), CS1729, CS1061 (×3) across `VolumeCarveTool.cs`, `BufferAsEditView.cs`, `VolumeProcessor.cs`, `VolumeObjectRegistry.cs`, and tests.
- **Dead code removal**: `RebuildDirty()` method had no callers — removed.

### Open / Next Steps
| Priority | Item | Details |
|----------|------|---------|
| 🔴 Critical | Verify Unity compile clean | Run in-editor, confirm zero errors after batch fixes |
| 🟡 High | Profiling baselines | Record meshing time for full/partial rebuilds with current resolution (128³) |
| 🟡 High | ADR-010 BuildTickets | Seam 2 — decouple sampling from meshing via ticket queue |
| 🟢 Medium | FillOperation / SmoothOperation | Stubbed but not implemented in Edit namespace |
| 🟢 Medium | Carve Tool testing | Verify raycast → carve → replay works end-to-end in Scene View |
| ⚪ Low | Test method rename | `VolumeProcessorAddObjectTests.cs` still references `RebuildDirty` in test names (cosmetic only) |

### Key Architecture Decisions
- **CommandStack**: Exklusiv für Composition Changes (Add/Remove/Clear VolumeObjects). Kein Unity-Undo-Hijacking.
- **PersistentEditLayer**: Separater cursor-basierter Stack für Voxel Edits (Carve/Fill). Aktiv nur wenn Carve Tool enabled (`EditorPrefs`).
- **ADR-001**: Rotation/Scale auf `_visualOutput`, nicht VolumeProcessor selbst. Grid bleibt axis-aligned.
- **Partial rebuild**: `MarkDirtyBounds(Bounds)` → `RebuildPipeline()` nutzt dirty region. Full Rebuild bei Transform-Change des Processors (Grid-Origin shift).

### Files Modified This Session
- `Assets/Scripts/VolumeSystem/Composition/VolumeProcessor.cs` — Major: undo hijacking removed, epsilon threshold, duplicate VisualOutput fix, dead code
- `Assets/Scripts/VolumeSystem/Composition/VolumeObjectRegistry.cs` — RebuildDirty → Rebuild()
- `Assets/Scripts/VolumeSystem/Core/Undo/CompositionCommand.cs` — ClearAllCommand added
- `Assets/Scripts/VolumeSystem/Edit/Editor/VolumeCarveTool.cs` — Full rewrite: 5 compile errors fixed, proper slider UI, wireframe cube, struct init
- `Assets/Scripts/VolumeSystem/Edit/BufferAsEditView.cs` — Added `using UnityEngine;`
- `Assets/Tests/Editor/VolumeProcessorAddObjectTests.cs` — RebuildDirty → Rebuild() (×3)

### Environment
- Unity 6000.4.1f1, Windows 10, branch `pipeline-rework`, ahead of origin by ~18 commits
